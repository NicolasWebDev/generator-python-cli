# CONFIGURATION -------------------------
_subdirectory: template
_tasks:
  - poetry add click
  - poetry add --dev pytest pytest-watch pytest-cov taskipy vulture black
  - git init
  - git add .
  - git commit -m 'First commit'
  - poetry run task test
  - poetry run task lint
# PROMPT --------------------------------
project_description: Makes excellent coffee.
